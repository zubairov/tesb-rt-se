package org.sopera.monitoring.mapping;

import java.util.ArrayList;
import java.util.List;

import junit.framework.Assert;

//import org.dozer.DozerBeanMapper;
import org.junit.Test;
import org.sopera.monitoring._2010._09.common.EventType;
import org.sopera.monitoring.event.Event;
import org.sopera.monitoring.util.EventCreator;

public class MapperTest {

	@Test
	public void mapTest() {
/*		List<String> mappingFiles = new ArrayList<String>();
		mappingFiles.add("dozer-global-configuration.xml");
		mappingFiles.add("dozer-event-eventType.xml");
		DozerBeanMapper mapper = new DozerBeanMapper(mappingFiles);

		Event e = new EventCreator().generateEvent();

		EventType et = (EventType) mapper.map(e, EventType.class);

		Assert.assertNotNull(et);*/

		// TODO write test

		return;
	}
}
